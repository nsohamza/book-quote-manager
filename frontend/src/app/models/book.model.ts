export interface Book {
  bookId: number;                // Optional for new books (not yet saved)
  title: string;              // Title of the book
  author: string;             // Author of the book
  publicationDate: string; 
}
